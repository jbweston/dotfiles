[general]
ui = ttyui
accounts = GMail, JBW
pythonfile = ~/.mutt/get_pass.py

[Account GMail]
localrepository = GMail-Local
remoterepository = GMail-Remote

[Account JBW]
localrepository = JBW-Local
remoterepository = JBW-Remote

[Repository GMail-Local]
type = Maildir
localfolders = ~/mail/GMail

[Repository JBW-Local]
type = Maildir
localfolders = ~/mail/JBW

[Repository GMail-Remote]
type = Gmail
ssl = yes
remoteuser = joseph.weston08@gmail.com
remotepasseval = get_pass("email/gmail")
folderfilter = lambda foldername: foldername not in ['[Gmail]/All Mail', '[Gmail]/Important']
realdelete = no
maxconnections = 3
sslcacertfile = /etc/ssl/certs/ca-certificates.crt

[Repository JBW-Remote]
type = IMAP
ssl = yes
remotehost = mail.webfaction.com
remoteuser = jbw
remotepasseval = get_pass("email/jbw")
realdelete = no
maxconnections = 3
sslcacertfile = /etc/ssl/certs/ca-certificates.crt
[Repository TUDelft-Remote]
type = IMAP
ssl = yes
remotehost = imap.tudelft.nl
remoteuser = jweston
remotepasseval = get_pass("email/tudelft")
realdelete = no
maxconnections = 3
sslcacertfile = /etc/ssl/certs/ca-certificates.crt
folderfilter = lambda f: f.startswith('INBOX') or f in ['Drafts', 'Sent' 'Trash', 'Archive']
